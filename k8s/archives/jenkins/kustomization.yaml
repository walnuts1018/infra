apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: jenkins
namePrefix: jenkins-
resources:
  - ingress.yaml
  - pvc.yaml
  - externalsecret.yaml
components:
  - ../../components/helm
patchesStrategicMerge:
  - helm-overlay.yaml

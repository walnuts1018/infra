apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: namespaces-biscuit
  namespace: argocd
  labels:
    argocd-agent: "true"
spec:
  project: biscuit
  destination:
    name: biscuit
  source:
    path: k8s/namespaces/biscuit
    repoURL: 'https://github.com/walnuts1018/infra'
    targetRevision: clusterapi
    directory:
      recurse: true
      exclude: '{config/*,_*/*,values*.yaml}'
      jsonnet: {}
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
    - ServerSideApply=true
    - FailOnSharedResource=true
    - Prune=confirm

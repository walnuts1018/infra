apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: namespaces-biscuit
  namespace: argocd
spec:
  project: default
  destination:
    name: biscuit
  source:
    path: k8s/namespaces
    repoURL: 'https://github.com/walnuts1018/infra'
    targetRevision: clusterapi
    directory:
      recurse: true
      exclude: '{config/*,_*/*}'
      jsonnet: {}
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
    - ServerSideApply=true
    - FailOnSharedResource=true

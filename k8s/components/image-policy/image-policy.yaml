apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: policy
  # namespace: to-be-filled
spec:
  imageRepositoryRef:
    #name: repo
  policy:
    #semver:
    # range: to-be-filled

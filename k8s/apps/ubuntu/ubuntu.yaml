apiVersion: apps/v1
kind: Deployment
metadata:
  name: ubuntu
spec:
  replicas: 100
  selector:
    matchLabels:
      app: "ubuntu"
  template:
    metadata:
      labels:
        app: "ubuntu"
    spec:
      containers:
        - name: ubuntu
          image: ubuntu
          command:
            - sleep
            - infinity

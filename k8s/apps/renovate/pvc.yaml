apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: renovate
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 1Gi
    storageClassName: longhorn

---

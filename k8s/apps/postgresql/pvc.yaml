apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc
spec:
  storageClassName: longhorn
  volumeName: postgresql
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi

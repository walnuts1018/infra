apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: postgres-
namespace: databases
resources:
  - pvc.yaml
  - sealedsecret.yaml
components:
- ../../components/helm
patchesStrategicMerge:
- helm-overlay.yaml
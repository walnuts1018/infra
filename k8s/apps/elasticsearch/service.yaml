apiVersion: v1
kind: Service
metadata:
    labels:
        app: elasticsearch
        app.kubernetes.io/name: elasticsearch
    name: elasticsearch
    namespace: elasticsearch
spec:
    ports:
        - port: 9200
          protocol: TCP
          targetPort: 9200
    selector:
        app: elasticsearch
        app.kubernetes.io/name: elasticsearch
    type: ClusterIP

---

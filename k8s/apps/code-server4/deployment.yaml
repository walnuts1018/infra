apiVersion: apps/v1
kind: Deployment
metadata:
  name: code-server4
  labels:
    app: code-server4
spec:
  replicas: 1
  selector:
    matchLabels:
      app: code-server4
  template:
    metadata:
      labels:
        app: code-server4
    spec:
      imagePullSecrets:
        - name: ghcr-login-secret
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: "OnRootMismatch"
      containers:
        - name: code-server4
          image: ghcr.io/kmc-jp/code-server-images-golang:7281f916c398452057c2784ee679fd07adfb2536-37 # {"$imagepolicy": "default:code-server4"}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 19200
          resources: {}
          args:
            - "--git-clone https://github.com/walnuts1018/http-dump"
          volumeMounts:
            - name: config
              mountPath: /home/coder/.config
            - name: work
              mountPath: /home/coder/work
            - name: local
              mountPath: /home/coder/.local
      nodeSelector:
        kubernetes.io/arch: amd64
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: code-server4-config
        - name: work
          persistentVolumeClaim:
            claimName: code-server4-work
        - name: local
          persistentVolumeClaim:
            claimName: code-server4-local

apiVersion: v1
kind: Service
metadata:
  labels:
    app: picca-frontend
    app.kubernetes.io/name: picca-frontend
  name: picca-frontend
  namespace: picca
spec:
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  selector:
    app: picca-frontend
    app.kubernetes.io/name: picca-frontend
  type: ClusterIP

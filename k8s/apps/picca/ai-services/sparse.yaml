apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: picca-sparse-service
    app.kubernetes.io/name: picca-sparse-service
  name: picca-sparse-service
  namespace: picca
spec:
  replicas: 1
  selector:
    matchLabels:
      app: picca-sparse-service
      app.kubernetes.io/name: picca-sparse-service
  template:
    metadata:
      labels:
        app: picca-sparse-service
        app.kubernetes.io/name: picca-sparse-service
    spec:
      containers:
        - command:
            - python
            - scripts/run_sparse_service.py
          env:
            - name: PORT
              value: "8002"
            - name: MODEL_DEVICE
              value: cpu
            - name: SPARSE_MODEL_NAME
              value: /models/light-splade-japanese-28M
          image: ghcr.io/walnuts1018/picca/ai-services:v0.0.49
          imagePullPolicy: IfNotPresent
          livenessProbe:
            failureThreshold: 3
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 10
          name: sparse-service
          ports:
            - containerPort: 8002
              name: http
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 10
          resources:
            limits:
              cpu: "1"
              memory: 1Gi
            requests:
              cpu: 250m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              add:
                - NET_BIND_SERVICE
              drop:
                - all
            readOnlyRootFilesystem: false
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - mountPath: /tmp
              name: tmp
            - mountPath: /models
              name: models
              readOnly: true
      imagePullSecrets:
        - name: ghcr-login-secret
      securityContext:
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
      serviceAccountName: picca
      volumes:
        - emptyDir: {}
          name: tmp
        - image:
            pullPolicy: IfNotPresent
            reference: ghcr.io/walnuts1018/picca/ai-models-sparse:v0.0.1
          name: models
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: picca-sparse-service
    app.kubernetes.io/name: picca-sparse-service
  name: picca-sparse-service
  namespace: picca
spec:
  ports:
    - name: http
      port: 8002
      targetPort: http
  selector:
    app: picca-sparse-service
    app.kubernetes.io/name: picca-sparse-service
  type: ClusterIP

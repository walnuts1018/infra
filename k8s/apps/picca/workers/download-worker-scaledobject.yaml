apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: picca-download-worker
  namespace: picca
spec:
  maxReplicaCount: 8
  minReplicaCount: 1
  scaleTargetRef:
    name: picca-download-worker
  triggers:
    - authenticationRef:
        name: picca-rabbitmq-worker-auth
      metadata:
        mode: ExpectedQueueConsumptionTime
        protocol: http
        queueName: picca.download
        value: "120"
      metricType: Value
      type: rabbitmq

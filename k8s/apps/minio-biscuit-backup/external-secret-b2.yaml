apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: minio-biscuit-backup-rclone-63cd09
  namespace: minio-biscuit
spec:
  data:
    - remoteRef:
        key: terraform-external-secrets
        property: b2-application-key
      secretKey: B2_APPLICATION_KEY
    - remoteRef:
        key: rclone
        property: b2_crypt_password
      secretKey: B2_ENCRYPTED_PASSWORD
    - remoteRef:
        key: rclone
        property: b2_crypt_salt
      secretKey: B2_ENCRYPTED_SALT
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: minio-biscuit-backup-rclone-63cd09
    template:
      engineVersion: v2
      type: Opaque

apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: default
  namespace: longhorn-system
spec:
  concurrency: 1
  cron: 0 4 * * ?
  groups: []
  labels:
    automated: "true"
  name: default
  retain: 1
  task: backup

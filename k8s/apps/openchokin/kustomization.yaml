apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - front.deployment.yaml
  - back.deployment.yaml
  - front.service.yaml
  - back.service.yaml
  - front.ingress.yaml
  - back.ingress.yaml
  - externalsecret.yaml
components:
  - ../../components/image-policy
patchesStrategicMerge:
  - front.image-policy-overlay.yaml
  - back.image-policy-overlay.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: openchokin-front
  labels:
    app: openchokin-front
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openchokin-front
  template:
    metadata:
      labels:
        app: openchokin-front
    spec:
      containers:
        - name: openchokin-front
          image: "ghcr.io/walnuts1018/openchokin-front:v0.0.0-0f8086a3c0e58032369b30a0d26e670e3b38679b-50" #{"$imagepolicy": "flux-system:openchokin-front-policy"}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          resources:
            limits: {}
            requests: {}
          env:
            - name: ZITADEL_URL
              value: "https://auth.walnuts.dev"
            - name: NEXTAUTH_URL
              value: "https://openchokin.walnuts.dev"
            - name: ZITADEL_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: openchokin-secret
                  key: zitade-client-id
            - name: ZITADEL_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: openchokin-secret
                  key: zitadel-client-secret
            - name: NEXTAUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: openchokin-secret
                  key: nextauth-secret

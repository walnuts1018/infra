apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ac-hacking-2024-back
spec:
  ingressClassName: "nginx"
  rules:
    - host: "ac-hacking-2024.walnuts.dev"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ac-hacking-2024-back
                port:
                  number: 8080

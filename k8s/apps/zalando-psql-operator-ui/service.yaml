apiVersion: "v1"
kind: "Service"
metadata:
  name: "zalando-postgres-operator-ui"
  labels:
    application: "zalando-postgres-operator-ui"
spec:
  type: "ClusterIP"
  selector:
    name: "zalando-postgres-operator-ui"
  ports:
    - port: 80
      protocol: "TCP"
      targetPort: 8081

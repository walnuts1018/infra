apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: "zalando-postgres-operator-ui"
  labels:
    application: "zalando-postgres-operator-ui"
spec:
  ingressClassName: "nginx"
  rules:
    - host: "zalando.walnuts.dev"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: "zalando-postgres-operator-ui"
                port:
                  number: 80

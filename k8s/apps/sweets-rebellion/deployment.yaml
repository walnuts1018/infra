apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
spec:
  selector:
    matchLabels:
      app: sweets-rebellion
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: sweets-rebellion
    spec:
      containers:
      - image: ghcr.io/kmc-jp/stgproject2022:ab10aa43c71a1331b37f1a955c7eadb319b5d2c2-28 # {"$imagepolicy": "flux-system:sweets-rebellion-policy"}
        imagePullPolicy: IfNotPresent
        name: sweets-rebellion
        ports:
        - containerPort: 80
          name: http
        volumeMounts:
        - mountPath: /etc/nginx/conf.d
          readOnly: true
          name: nginx-conf
        - mountPath: /var/log/nginx
          name: log
      imagePullSecrets:
      - name: ghcr-login-secret
      volumes:
      - name: nginx-conf
        configMap:
          name: sweets-rebellion-nginx-conf
          items:
          - key: default.conf
            path: default.conf
      - name: log
        emptyDir: {}

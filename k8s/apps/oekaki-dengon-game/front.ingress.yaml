apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: oekaki-dengon-game-front
spec:
  ingressClassName: "nginx"
  rules:
    - host: "oekaki.walnuts.dev"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: oekaki-dengon-game-front
                port:
                  number: 3000

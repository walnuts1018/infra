apiVersion: v1
data:
  affine.js: |
    AFFiNE.use("oauth", {
      providers: {
        oidc: {
          // OpenID Connect
          issuer: "",
          clientId: "",
          clientSecret: "",
          args: {
            scope: "openid email profile",
            claim_id: "preferred_username",
            claim_email: "email",
            claim_name: "name",
          },
        },
      },
    });
kind: ConfigMap
metadata:
  labels:
    app: affine
    app.kubernetes.io/name: affine
  name: affine-fc340a
  namespace: affine

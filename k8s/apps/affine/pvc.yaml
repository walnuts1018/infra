apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: affine-storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: longhorn
  volumeName: affine-storage

apiVersion: v1
kind: Service
metadata:
  labels:
    app: affine
    app.kubernetes.io/name: affine
  name: affine
  namespace: affine
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app: affine
    app.kubernetes.io/name: affine
  type: ClusterIP

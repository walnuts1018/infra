apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: stalwart-mx
  namespace: stalwart
spec:
  dnsNames:
    - mx.walnuts.dev
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: stalwart-mx-tls

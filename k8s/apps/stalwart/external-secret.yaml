apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: stalwart-6291b9
spec:
  data:
    - remoteRef:
        key: postgres_passwords
        property: stalwart
      secretKey: postgres_password
    - remoteRef:
        key: seaweedfs
        property: stalwart_secretkey
      secretKey: s3_secret_access_key
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: stalwart-6291b9
    template:
      engineVersion: v2
      type: Opaque

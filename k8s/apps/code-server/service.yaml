apiVersion: v1
kind: Service
metadata:
  name: code-server
  labels:
    app: code-server
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: code-server
  type: ClusterIP

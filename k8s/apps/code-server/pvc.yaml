apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: code-server
spec:
  storageClassName: longhorn
  volumeName: code-server
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

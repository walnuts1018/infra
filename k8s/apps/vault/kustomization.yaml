apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: vault-
namespace: vault
resources:
  - sa.yaml
components:
  - ../../components/helm
patchesStrategicMerge:
  - helm-overlay.yaml

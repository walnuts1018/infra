apiVersion: sts.min.io/v1alpha1
kind: PolicyBinding
metadata:
  name: terrakube-registry
  namespace: minio
spec:
  application:
    namespace: terrakube
    serviceaccount: terrakube-registry
  policies:
    - readwrite

apiVersion: v1
kind: Service
metadata:
  name: backup-manager
  labels:
    app: backup-manager
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: backup-manager
  type: ClusterIP
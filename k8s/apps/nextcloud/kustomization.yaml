apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: nextcloud-
namespace: default
resources:
  - externalsecret.yaml
  - pvc.yaml
components:
  - ../../components/helm
patchesStrategicMerge:
  - helm-overlay.yaml

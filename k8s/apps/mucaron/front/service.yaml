apiVersion: v1
kind: Service
metadata:
  labels:
    app: mucaron-front
    app.kubernetes.io/name: mucaron-front
  name: mucaron-front
  namespace: mucaron
spec:
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: mucaron-front
    app.kubernetes.io/name: mucaron-front
  type: ClusterIP

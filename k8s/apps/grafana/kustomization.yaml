apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
namePrefix: grafana-
resources:
  - externalsecret.yaml
  - ingress.yaml
components:
  - ../../components/helm
patchesStrategicMerge:
  - helm-overlay.yaml

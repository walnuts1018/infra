global:
  storageClass: "longhorn"
  postgresql:
    username: "admin"
    existingSecret: "postgres-ha-secret"
  pgpool:
    adminUsername: "admin"
    existingSecret: "postgres-ha-secret"

service:
  type: LoadBalancer
  loadBalancerIP: "192.168.120.17"

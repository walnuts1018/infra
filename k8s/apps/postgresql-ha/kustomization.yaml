apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: postgres-ha-
namespace: databases
resources:
  - externalsecret.yaml
components:
  - ../../components/helm
patchesStrategicMerge:
  - helm-overlay.yaml
configMapGenerator:
  - name: helm-values
    namespace: databases
    files:
      - values.yaml

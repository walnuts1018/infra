apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: archivebox
spec:
  storageClassName: longhorn
  volumeName: archivebox
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi

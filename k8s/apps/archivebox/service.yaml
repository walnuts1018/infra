apiVersion: v1
kind: Service
metadata:
  name: archivebox
  labels:
    app: archivebox
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app: archivebox
  type: ClusterIP

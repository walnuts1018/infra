apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: minio-default-backup-aws-bf1fa7
  namespace: minio
spec:
  data:
    - remoteRef:
        key: minio-default-backup
        property: minio_biscuit_secret_key
      secretKey: minio_biscuit_secret_key
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: minio-default-backup-aws-bf1fa7
    template:
      data:
        credentials: |
          [minio-biscuit]
          aws_access_key_id = JuMzGU8z4QVEo97fu3it
          aws_secret_access_key = {{ .minio_biscuit_secret_key }}
      engineVersion: v2
      type: Opaque

---
apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: HelmRepository
metadata:
  name: repo
spec:
  url: https://oauth2-proxy.github.io/manifests
---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: release
spec:
  chart:
    spec:
      chart: oauth2-proxy
      version: 6.24.2
      sourceRef:
        kind: HelmRepository
        name: kibana-oauth2-proxy-repo
  values:
    config:
      existingSecret: kibana-oauth2-proxy-secret
      configFile: |-
        email_domains = [ "*" ]
        upstreams = [ "http://kibana.default.svc.cluster.local:5601" ]
        pass_access_token = true
        user_id_claim = "sub"
        oidc_groups_claim="my:zitadel:grants"
        allowed_groups = ["237477822715658605:kibana-admin"]

    extraArgs:
      provider: oidc
      redirect-url: https://kibana.walnuts.dev/oauth2/callback
      oidc-issuer-url: https://auth.walnuts.dev
      skip-provider-button: true
    ingress:
      enabled: true
      className: nginx
      path: /
      pathType: Prefix
      hosts:
        - "kibana.walnuts.dev"
    sessionStorage:
      type: redis
      redis:
        existingSecret: "kibana-oauth2-proxy-secret"
        passwordKey: "redis-password"
        clientType: "sentinel"
        sentinel:
          existingSecret: "kibana-oauth2-proxy-secret"
          passwordKey: "redis-password"
          masterName: "mymaster"
          connectionUrls: "redis://redis-release.databases.svc.cluster.local:6379,redis://redis-release.databases.svc.cluster.local:26379"
    metrics:
      enabled: true

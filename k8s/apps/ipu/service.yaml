apiVersion: v1
kind: Service
metadata:
  labels:
    app: ipu
    app.kubernetes.io/name: ipu
  name: ipu
  namespace: ipu
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
  selector:
    app: ipu
    app.kubernetes.io/name: ipu
  type: ClusterIP

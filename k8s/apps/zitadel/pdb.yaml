apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    app: zitadel
    app.kubernetes.io/name: zitadel
  name: zitadel
  namespace: zitadel
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: zitadel
      app.kubernetes.io/name: zitadel

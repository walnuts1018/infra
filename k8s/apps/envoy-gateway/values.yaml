deployment:
  envoyGateway:
    resources:
      requests:
        cpu: 2m
        memory: 51Mi
      limits:
        memory: 1Gi

config:
  envoyGateway:
    extensionApis:
      enableBackend: true

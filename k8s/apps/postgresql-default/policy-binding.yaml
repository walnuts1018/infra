apiVersion: sts.min.io/v1alpha1
kind: PolicyBinding
metadata:
  name: postgresql-default
  namespace: minio
spec:
  application:
    namespace: databases
    serviceaccount: postgresql-default
  policies:
    - readwrite

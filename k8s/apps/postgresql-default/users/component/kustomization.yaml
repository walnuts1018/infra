apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
patches:
  - path: patches.yaml
    target:
      group: external-secrets.io
      kind: ExternalSecret
configurations:
  - configurations.yaml

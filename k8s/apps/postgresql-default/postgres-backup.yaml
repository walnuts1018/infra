apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgresql-default-postgresql-backup
spec:
  backupOwnerReference: self
  cluster:
    name: postgresql-default
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
  schedule: 0 0 19 */7 * *

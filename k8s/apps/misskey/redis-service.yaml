apiVersion: v1
kind: Service
metadata:
  name: misskey-redis
  labels:
    app: misskey-redis
spec:
  ports:
    - port: 6379
      protocol: TCP
      targetPort: 6379
  selector:
    app: misskey-redis
  type: ClusterIP

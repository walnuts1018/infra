apiVersion: v1
kind: Service
metadata:
  name: misskey-web
  labels:
    app: misskey-web
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app: misskey-web
  type: NodePort

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: misskey-pvc
spec:
  storageClassName: longhorn
  volumeName: misskey
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 4Gi

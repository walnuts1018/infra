apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: misskey
spec:
  ingressClassName: "nginx"
  rules:
    - host: "misskey.walnuts.dev"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: "misskey"
                port:
                  number: 80

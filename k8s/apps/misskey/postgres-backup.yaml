apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: misskey-postgresql-backup
spec:
  backupOwnerReference: self
  cluster:
    name: misskey-postgresql
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
  schedule: 0 0 18 */3 * *

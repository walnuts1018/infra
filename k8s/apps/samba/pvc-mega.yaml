apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mega
  namespace: samba
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 150Gi
  storageClassName: longhorn-local-encrypted

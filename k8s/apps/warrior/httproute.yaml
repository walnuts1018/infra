apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: warrior
  namespace: warrior
spec:
  hostnames:
    - warrior.walnuts.dev
  parentRefs:
    - name: envoy-gateway
      namespace: envoy-gateway-system
  rules:
    - backendRefs:
        - kind: Service
          name: warrior
          port: 8001
          weight: 1

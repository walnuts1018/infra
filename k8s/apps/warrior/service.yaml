apiVersion: v1
kind: Service
metadata:
  labels:
    app: warrior
    app.kubernetes.io/name: warrior
  name: warrior
  namespace: warrior
spec:
  ports:
    - name: http
      port: 8001
      protocol: TCP
      targetPort: http
  selector:
    app: warrior
    app.kubernetes.io/name: warrior
  type: ClusterIP

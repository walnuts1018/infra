kind: Service
apiVersion: v1
metadata:
  name: cloudflared
  labels:
    app: cloudflared
    app.kubernetes.io/name: cloudflared
spec:
  selector:
    app: cloudflared
  ports:
  - protocol: TCP
    port: 60123
    targetPort: 60123
  type: ClusterIP

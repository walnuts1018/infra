apiVersion: bootstrap.cluster.x-k8s.io/v1alpha3
kind: TalosConfigTemplate
metadata:
  name: longhorn-backup-validateworker
spec:
  template:
    spec:
      generateType: join
      talosVersion: v1.11.5

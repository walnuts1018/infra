apiVersion: v1
kind: Service
metadata:
  name: fitbit-manager
  labels:
    app: fitbit-manager
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: fitbit-manager
  type: LoadBalancer

apiVersion: v1
kind: Service
metadata:
  labels:
    app: fitbit-manager
    app.kubernetes.io/name: fitbit-manager
  name: fitbit-manager
  namespace: fitbit-manager
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: fitbit-manager
    app.kubernetes.io/name: fitbit-manager
  type: ClusterIP

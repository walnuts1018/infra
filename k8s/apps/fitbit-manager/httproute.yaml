apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: fitbit-manager
  namespace: fitbit-manager
spec:
  hostnames:
    - fitbit.walnuts.dev
  parentRefs:
    - name: envoy-gateway
      namespace: envoy-gateway-system
  rules:
    - backendRefs:
        - kind: Service
          name: fitbit-manager
          port: 8080
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /

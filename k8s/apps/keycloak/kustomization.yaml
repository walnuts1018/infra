apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: keycloak-
resources:
  - sealedsecret.yaml
  - ingress.yaml
  #- vpa.yaml
components:
- ../../components/helm
patchesStrategicMerge:
- helm-overlay.yaml

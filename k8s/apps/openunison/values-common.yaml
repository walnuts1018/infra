# Common values shared between orchestra and orchestra-login-portal charts
network:
  openunison_host: k8s-openunison.local.walnuts.dev
  # OpenUnison validates a distinct dashboard hostname even when its dashboard is disabled.
  dashboard_host: dashboard-disabled.local.walnuts.dev
  api_server_host: kurumi-kubernetes-api.local.walnuts.dev
  session_inactivity_timeout_seconds: 900
  k8s_url: https://kubernetes.default.svc
  force_redirect_to_tls: true
  createIngressCertificate: true
  ingress_type: gatewayapi
  ingress_annotations: {}
  gatewayapi:
    gateway:
      create: false
      parent_refs:
      - name: envoy-gateway
        namespace: envoy-gateway-system
        sectionName: https

cert_template:
  ou: Kubernetes
  o: walnuts.dev
  l: Kurumi
  st: Tokyo
  c: JP

myvd_config_path: WEB-INF/myvd.conf
k8s_cluster_name: kurumi
enable_impersonation: true

impersonation:
  use_jetstack: false

dashboard:
  enabled: false

certs:
  use_k8s_cm: false

trusted_certs: []

network_policies:
  enabled: false

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: network-exporter
resources:
- deployment.yaml
- externalsecret.yaml
configMapGenerator:
- name: cloudflare-ddns-config-base
  files:
  - config.json
generatorOptions:
  disableNameSuffixHash: true

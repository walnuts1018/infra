apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: http-dump-oidc-secret-36f791
spec:
  data:
    - remoteRef:
        key: http-dump
        property: client-secret
      secretKey: client-secret
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: http-dump-oidc-secret-36f791

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: http-dump-oidc-secret-d0b3c1
spec:
  data:
    - remoteRef:
        key: http-dump
        property: client-secret
      secretKey: client-secret
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: http-dump-oidc-secret-d0b3c1

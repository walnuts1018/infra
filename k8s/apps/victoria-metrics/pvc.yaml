apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc
spec:
  storageClassName: longhorn
  volumeName: victoria-metrics
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi

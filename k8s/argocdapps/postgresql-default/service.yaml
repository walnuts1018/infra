apiVersion: v1
kind: Service
metadata:
    labels:
        application: spilo
        cluster-name: default
        spilo-role: master
        team: default
    name: postgresql-default
spec:
    loadBalancerIP: 192.168.0.134
    ports:
        - name: postgresql
          port: 5432
          targetPort: 5432
    selector:
        application: spilo
        cluster-name: default
        spilo-role: master
        team: default
    type: LoadBalancer

---

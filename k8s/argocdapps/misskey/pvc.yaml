apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: misskey
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 4Gi
    storageClassName: longhorn
    volumeName: misskey

---
